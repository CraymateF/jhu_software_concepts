[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    db: tests that require a database connection
    web: tests for Flask web routes
    buttons: tests for button/action endpoints
    analysis: tests for analysis output formatting
    integration: end-to-end integration tests
