# Flask Web Framework
Flask==3.1.3
# Pin Jinja2 to fix XSS/Template Injection vulnerabilities
Jinja2==3.1.6

# PostgreSQL Database Adapter  
psycopg2-binary==2.9.9
# Modern PostgreSQL adapter (for SQL composition)
psycopg==3.3.3

# Environment variable management
python-dotenv==1.2.1

# Web Scraping Dependencies
beautifulsoup4==4.12.3
lxml==5.1.0

# Note: LLM dependencies (llama-cpp-python, huggingface_hub) removed
# They are only used in legacy module_2_code and have security vulnerabilities
# Install separately if you need the LLM data standardization features

# Testing Dependencies
pytest==7.4.3
pytest-cov==4.1.0
pytest-timeout==2.2.0
pytest-xdist==3.5.0  # Parallel test execution

# Documentation Dependencies
sphinx==7.2.6
sphinx-rtd-theme==2.0.0

# Code Quality and Analysis Tools
pylint==4.0.5
pydeps==3.0.2

# Python version requirement: 3.10+
zipp>=3.19.1 # not directly required, pinned by Snyk to avoid a vulnerability
